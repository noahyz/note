## 费用流和算法

给定一个网络 `G=(V, E)`，每条边有容量限制 `c(u, v)`，还有单位流量的费用 `w(u, v)`。

当 `(u, v)` 的流量为 `f(u, v)` 时，需要花费 `f(u, v) * w(u, v)` 的费用。该网络中总花费最小的最大流称为“最小费用最大流”，总花费最大的最大流称为“最大费用最大流”，二者合称“费用流”模型，即在最大流的前提下考虑费用的最值。

