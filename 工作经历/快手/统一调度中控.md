---
title: 统一调度中控
---

### 一、背景

快手静态流量调度长期多系统割裂、多组件各自决策且配置强依赖人工盘点与调参，导致带宽不可控、三方兜底比例偏高并带来成本与稳定性风险。

| 事项                                   | 背景&目标                                                    |
| -------------------------------------- | ------------------------------------------------------------ |
| 打通PCDN和CDN后端                      | 24年跨省结算等政策，导致PCDN资源供给不足，查种失败率晚高峰从90%到60%，希望通过自建CDN补PCDN的量。<br />1. 使用PCDN，给自建CDN机房增量。填谷、填保底。 利用自建CDN剩余利用率，将95机房跑到特定水位线模式。需要自建支持跨省能力。<br />2. 使用自建CDN给PCDN补量。 |
| 自建机房上量，减少三方依赖（价格便宜） | 1. 建设DNS调度能力，承接静态量。<br />2. 推进业务接入自建CDN上量，从而通过自建调度实现在三方CDN和自建CDN之间的打通，提升不同资源间打通的程度。 |
| 自建调度能力不足                       |                                                              |

调度模式缺陷

-  之前的调度模式为“自建接不住，无脑漏三方”，导致自建、三方都无法利用计费策略做功，控制成本收益。
-  SRE 需要手动计算分配流量，但由于可调节参数较多(文件区间、下发比、带宽控制线、权重等)，导致无法以全局视角合理的给出最优解
-  压制算法是纯反馈式的，且是按照上一次的压制结果按比例调节，导致受统计带宽的波动而出现抖动巨大，没有考虑历史带宽，无法预测式调节
-  大部分组件是“类执行器”形态的，多个组件各自作用于同一资源（机房出口之类），组件之间互相不感知决策策略，仅感知决策结果，收敛慢且容易产生策略打架，导致跑量没有精度可言。遇到跑量不精准的场景，无能为力，导致调度结果进一步劣化
-  没有区分业务，缺乏不同业务间差异化调度的能力

调度控制能力不足：

- 有大量的业务并没有接入调度SDK。还是代码写死域名的形式。
- 缺乏数据基建，实际跑量和调度预期不符，且差异未知。
  -  调度预期某个业务全部在自建，但实际有很多在三方，也不清楚其中多少是调度问题，多少是业务放量问题
  -  设备建设规划缺少数据指导，不知道哪些地区加了机器调度可以跑进来量，哪些地区加了之后会造成机房浪费
- 自建CDN没有跨省调度能力。自建静态机器省份运营商和业务分布不完全匹配，但放量受制于运维配置的规则，大部分都是本省覆盖无法跨省。
- 同一个端口不同业务调入，分别受制于各自调度规则，存在整体不够，一方有量一方不足，但有量方不能补齐缺量方
- 没有CDN业务侧的整体调配策略，单纯的只压制点播、直播不压制的思路。导致机房跑量不可控，存在一定的成本浪费

沉重的历史技术债务：

- 没有统一的全局控制带宽能力。比如：
  - ECDN在客户端接入侧高优抢量，和自建、三方CDN之间没有联动能力
  - 自建保低不足，但很多业务只能在三方CDN跑，无法回到自建导致浪费
  - 自建的预期调度下发比例结果和实际的跑量带宽不符，且不同地域不同业务之间有不同的结果。
- 历史遗留逻辑非常繁重，调度SDK非常臃肿，业务逻辑、放量逻辑各种耦合，稳定性风险极高，还有性能问题
- 老调度SDK臃肿复杂的逻辑导致一部分本应该接入自建的流量漏到三方，且想要拿到这部分流量收益，缺乏数据支持，推进非常困难。
  - 比如TV业务的photoId参数传入负数，导致有400G+的流量漏到三方
- 客户端的预取、升降码率等问题导致点播的文件大小，在不同省份运营商、免流类型等多个维度下都可能不同。
- 调度下发的结果，中间的链路非常长，任意一个地方都有可能出现问题。比如：
  - 调度URL上携带参数用于区分业务带宽，但是客户端截取参数、节点侧去参操作等等导致业务带宽不准，无法调度。
- 点播、直播、静态多套调度系统，互相之间策略打架，点播可用流量抖动严重，回源问题较为突出。

运维成本巨大：

- 重度依赖“文件下发比”做调度调量，调度主要能力人工化严重。如下举例：
  - 某些节点裁撤，没有补上裁撤后的缺口。导致文件下发比没有打开，需要人工调整文件下发比调量。
  - 月初调线，需要SRE和策略同学耗费至少6天的时间调整文件下发比，进行控量。
- 缺乏可用的调度数据。如下举例：
  - 基于负反馈的历史调度模式，导致产出的数据无法划清问题分类，那些是调度问题？那些是业务问题
  - 资源建设缺乏数据指导，不知道哪些地区加了机器调度可以跑进来量，哪些地区加了之后会造成机房浪费
- 大量的非标资源接入方式比较特化，抽象化的建设不足，导致逻辑异常复杂。

### 二、产出

- 释放自建可控带宽 6T+；点播自建增量 1T+，静态混跑增量 2T+
- 点播回源率（NC→NS）23%→18%，（NS→L2）10%→8%。
- 机房日均浪费 577G→140G，月均浪费率 3%→0.65%。
- 非直播自建完成统一调度（点播IP调度，静态DNS调度），并推全所有自建机房。清理了历史技术债务，降低了运维成本。
- 资源需求转化模型上线基本跑通，调度可控带宽数据建设健全，实现资源与需求的动态匹配。系统化输出调度表，清晰展示各业务使用的自建与三方资源及占比。
- 新模型推动了依赖限制条件量化调度，通过优化架构，调度中控基本完成了数据驱动、智能化、白盒化；节省运维人力。
- 提升了调度控制能力，文件比控制回源率，下发比控制机房带宽。通过PID算法减少机房带宽抖动，自研槽位哈希算法解决了机房回源问题。

### 3.  解决路径

项目立项：围绕“统一控制平面+自动化调度”明确范围与里程碑，建立可度量目标（成本优化、架构扩展、运营效率等），按业务/资源/地域拆分推进路径，并同步风险与回滚预案。
架构收敛：多入口、多组件能力收敛为单一控制面，统一资源抽象与策略表达，消除并行控制同一资源导致的策略打架，推动调度白盒化。
调度模式：落地“IP调度(资源级精准调度)+DNS调度(域级覆盖)”双模式接入，提升资源级可控覆盖，从架构、质量、成本等多维实现自适应。
决策算法：以最小费用最大流算法做全局规划分配（前馈），叠加增量式 PID 闭环抑制波动并提升收敛，通过自研离散型槽位哈希算法降低节点变更带来的迁移与扰动。
模型预测：基于决策树集成学习（GBDT）做大规模特征工程，构建可控带宽预测服务，用“预测值+在线校准”对抗输入噪声与场景差异。

---

调度中控的上游包括：带宽数据（可控带宽+机器实时带宽）、机房配置数据。可控带宽=文件大小 * 调度QPS

调度中控通过“费用流算法”做全局规划分配（前馈）通过“PID算法”做负反馈。

```
如果算法执行时间很久，也会影响秒级调度能力。算法覆盖的范围越大，粒度越细，能够求得的解更优，但往往也需要更多的时间。
常见的费用流是类dinic算法的O(VEF)时间复杂度，V为节点数，E是边数，F是总流量。

CDN的带宽往往是Tbps级别，到请求粒度也是上亿qps，如果F到qps粒度，则时间消耗会很久。
基于此特点，快手CDN采用基CapacityScale的弱多项式费用流算法，可以实现 O(E^2 * logU * logV) 的复杂度，将数量级最大的U即容量做到对数复杂度，使得请求粒度的最优解可以在秒级完成求解。
另外快手CDN有根据业务场景进行了缩边、缩点等算法优化，最终使得在秒级可以做到全国范围内请求粒度最优求解。
```

自研离散型槽位哈希算法：

```
https://noahyz.github.io/%E6%8A%80%E6%9C%AF%E4%B8%93%E9%A2%98/%E4%B8%80%E8%87%B4%E6%80%A7%E5%93%88%E5%B8%8C/1.%E8%87%AA%E7%A0%94%E7%A6%BB%E6%95%A3%E5%9E%8B%E6%A7%BD%E4%BD%8D%E5%93%88%E5%B8%8C.html#_5-%E6%A7%BD%E4%BD%8D%E9%87%8D%E5%B9%B3%E8%A1%A1

优点：
1. 查询性能、分布均匀性都不错
2. 精确权重映射
3. 节点变动时，无需重映射，做到零扰动
4. 可以在任意位置增删节点
5. 支持工程介入配置，人工可控，可以灵活调优
6. 重平衡时机可控，粒度可控

缺点：有状态

适用场景：节点配额精确匹配，节点扩缩容导致的重映射场景要求无扰动、人工可控，可以容忍带状态的场景。
在负载均衡、单调性、稳定性（扰动量）、支持权重、查找复杂度、空间开销等方面都比其他一致性哈希算法优秀。
```

平滑可控带宽（去躁点，基于决策树的机器学习算法）

---

调度模式的根本转变（自建调度正式进入系统化、模型化调优阶段）

- 调量方式：**手动调整文件区间时代 -> 系统自动计算文件比**。
- 调度模型：从“纯负反馈调度” -> “流模型+槽位哈希+前馈+负反馈”
- 运维模式：从“事后调参救火” -> “事前预测+在线平滑+白盒可控”

提升了调度控制能力：

- 建设了统一的、标准的CDN调度大中控，将之前的各种决策中心进行合并，从而进行统一调度。实现不同业务间、不同资源间、不同省份间的彻底打通。并已经运用在点播、静态调度服务。
- 基于费用流模型，使调度具备前馈+负反馈的调度能力。前馈为不同资源分配流量，反馈负责实时的微调流量占比。**无需SRE手动计算分配流量，使调度具备主动分配流量的能力，可以全局计算最优解。**
- 基于点播、直播、静态等CDN形态，统一了费用流模型。并支持了点播、静态的混跑项目，为深挖机器跑量能力、极致优化成本提供了能力。
- **针对“机房回源率”和“机房跑量”等指标之间存在冲突问题，优化流模型算法，将单纯的“分配比”拆分为“文件比”和“下发比”。“文件比”用于控制机房回源率，“下发比”用于控制机房跑量。在多个互相矛盾的机房指标中，挖掘了较为均衡、且扩展性强的调度模式**。
- 业界的哈希算法不足以满足快手复杂的调度业务场景。参考了“ring-hash”、“rendezvous-hash”、“anchor-hash”、“maglev-hash”、“jump-consistent-hash”等等业界著名的一致性哈希算法。**最终设计了“离散性槽位哈希算法”，能够有效降低机房回源率、并且增加机房跑量，可以自动有效的调整文件比。已经落地到自建CDN超过200+机房，并且在线上验证了2个多月，稳定性非常高**。
- 针对调度压制的平滑处理，调研了 “Moving average移动平均算法”、“Exponential Smoothing指数平滑算法”、“Kalman filter卡尔曼滤波算法”、“PID算法”等，在实际求解过程中，从代码实现、测试结果来看，增量式的PID算法最适合调度带宽跑量的平滑处理。**之前100G的机房压制时抖动幅度可达5G左右，通过PID算法平滑处理后抖动幅度降低到800M以内**。
- 统一调度中控，我们可以将各种决策组件进行合并，将原本的决策中心作为输入，彻底实现不同业务间、不同资源间、不同省份间的彻底打通。目前已经完成非直播业务的统一接入。
- 及时发现并支持线上缺失的调度能力。支持针对95机房的保底率，避免产生成本浪费；针对点播的日志带宽和实际带宽之间的差异，按照不同省份以差异化系数打平，保证基建数据尽可能准确。

**提升了调度精准度**

- 将调度下发比下沉到调度SDK，通过负反馈的方式实时指导调度。流模型负责粗调带宽，负反馈则更加精准的控制带宽。机房点播带宽波动由之前10%的误差降低到1%。
- 平滑点播可控带宽（去躁点，基于决策树的机器学习算法），可控带宽基本平滑&准确。流模型计算更加准确，
- 资源池划分粒度更细，精确点播、静态的带宽需求量级。让带宽控制粒度更加精细

**清理了历史技术债务**

- 通过节点日志上报带宽，可以计算出不同业务在不同省份运营商下的文件大小，结合调度的下发量，得出各省份运营商各业务的可控带宽。让调度的数据更加健全，使调度的输入更加精准，调度更加有效。
- 资源需求转化模型上线基本跑通，调度可控带宽数据建设健全，整合出较为清晰的调度表。“某个业务” 分配了 “哪些自建资源”、“哪些三方资源”，以及占比和带宽。彻底看清每一份调度流量来源去向。
- 自建、三方资源统一调配，策略就可以根据计费策略统一做功，拿收益。
- 调度可以针对业务级别，做差异化的调度
- 调度可以根据业务级别的带宽、QPS，更加合理、公平的进行调度，从而最大可能的使用好 CDN 资源

**降低了运维成本**

- 可以得出统一的资源规划数据，用于指导资源侧建设机房。
- 如果某自建机房保底不足，可以及时由 调度中控 调高此自建机房的比例，分配给相应的业务。从而保证自建资源无浪费。此操作不再需要SRE手动进行操作。
- 调度白盒化(比如console展示下发比和文件比信息)，提供了运维外部干预调度执行结果的能力，能够深度有效的人工控制调度执行。
- 调度智能化，机房的文件比、下发比都通过合适的限制条件自动计算，结束了人工调度















