

````
1. 容量调度配置，单独的一个线程，去拉取相关配置
CapacitySched(16999, SchedServiceGrpc.class, true, true, "public-cdn-dispatch-capacity")
容量调度，暂时不用了。拉取配置一般返回的为空。直接走路由策略了。

2. 
````



```
Kwai 调度
1. 查看 project 对应的配置，是否禁用自建。如果禁用，则跳过自建
2. 选择自建节点
	- 仅大陆地区可以走自建。非大陆地区跳过自建
	- 超热点不走自建
	- 是否为特殊配置，不满足要求，则不走自建
	- 请求命中了实验，如果实验不是自建的实验，则跳过自建
3. 通过调度表查询对应的节点

如果从调度表没有找到，则转到海外自建 CDN 节点
1> 海外不支持免流，对于免流请求，则跳过自建
2> 按照请求的地域、运营商等各种维度，进行匹配
3> 通过海外的调度表，来查询对应的节点。根据一些条件，选择合适的节点

outsourcing 调度



```

